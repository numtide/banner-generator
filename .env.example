# GitHub API token for fetching repository data
# Get one from: https://github.com/settings/tokens
GITHUB_TOKEN=

# API Server Configuration
PORT=8080

# Font directory (relative or absolute path)
FONT_DIR=fonts

# Web fonts configuration
# Set to true to use web fonts instead of embedded fonts
WEB_FONTS=false
# Base URL for web fonts (if not set, uses server URL)
BASE_URL=

# Access control (comma-separated list)
# Examples:
# - Allow specific orgs: "numtide,nixos"
# - Allow specific repos: "torvalds/linux,rust-lang/rust"
# - Mix of both: "numtide,torvalds/linux"
ALLOW_LIST=